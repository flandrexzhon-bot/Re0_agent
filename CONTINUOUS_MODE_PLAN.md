# Re0Agent 连续世界模式迁移计划

## 1. 目标

将当前六段式大回合彻底迁移为持续运行的事件世界。玩家输入不再是世界推进的必要触发器；在玩家沉默时，在场角色仍会依据自己的目标、关系、情绪、日程和未完成事项自主行动、彼此交流，并主动与主角互动。

连续模式不等于无状态聊天。系统仍需使用内部原子事件保证因果一致性、暂停、插话、存档、分支、重生成、死亡回归、规则结算和记忆写入，但不再向玩家暴露固定回合与六阶段流程。

本计划的核心是“双导演”体系：

- **SceneDirector（隐藏导演）**负责宏观剧情节奏、事件机会、时间推进、场景选择和镜头规划。
- **开普勒（显示导演）**负责把后台规划转化为玩家可见的环境叙述、镜头落点、转场和微观节奏。
- **Character Agent**只负责自己的语言、动作和主观感受，不承担权威环境描写、镜头切换或时空转移。

## 2. 已确认的产品决策

1. GM 概念退出前台；原 GM 的叙事身份保留为开普勒。
2. 世界采用动态事件调度，不使用固定间隔，也不要求等待玩家输入。
3. 游戏提供暂停按钮；暂停时停止世界时间、事件调度、离屏模拟和新的 LLM 调用。
4. 游戏包含剧场模式与 RP 模式。
5. 角色记忆继续按角色隔离，角色不能看到章节剧情或不应知道的离屏事实。
6. SceneDirector 和开普勒可以看到章节规划，但章节只能作为软方向，不能覆盖已经发生的事实或强行把剧情拉回预定轨道。
7. 原提示词中的角色设定、文风和世界观内容尽量保留；所有依赖“本回合、GM 开场、位号、上一回合”的编排契约必须重写。
8. 支持 SillyTavern 角色卡和世界书核心规范，但不执行第三方插件。
9. 世界书、人物资料和角色记忆采用确定性规则与向量检索结合的混合检索。
10. 剧场模式的玩家输入是给双导演的创作指令，不是主角行为或台词。
11. SillyTavern `first_mes` 和选中的备用问候原样展示。
12. 未发布旧版本的游戏存档直接删除，不建设兼容迁移；非存档用户配置继续保留。
13. SceneDirector 采用两级运行：每个前台事件和被提升的重要离屏事件后运行轻量脉冲判断，关键节点运行完整规划与反思。
14. `PlayerDirection` 的核心意图必须因果化实现；可以先铺垫和等待合理时机，但不能被角色普通自治目标静默忽略。
15. 剧场模式的离屏事件按世界因果立即发生，由 Director 择机揭示；已经对当前镜头产生可观察后果的事件不得延迟。
16. 游戏模式在创建会话时确定并永久锁定；同一会话内不允许在剧场模式与 RP 模式之间切换。
17. 世界速度是逻辑世界时间相对现实时间的倍率，不只是界面播放速度；玩家输入或 STT 期间，全世界进入真实慢动作。
18. 死亡回归默认只有世界规则指定的回归者保留被回滚循环中的记忆；其他角色记忆随世界状态回滚。
19. 普通离屏日程和规则事件不调用 DirectorPulse，由确定性调度器处理；只有被精度提升的重要离屏事件进入 Pulse。

## 3. 两种游戏模式

`game_mode` 是 `ChatSession` 的不可变创建参数。界面只允许在新建会话时选择模式；切换模式必须创建新会话，不能复用原会话的玩家知识、主角控制权或隐藏事件可见性状态。

### 3.1 剧场模式

- 主角拥有自己的 Character Agent，与其他角色一样可以自主行动。
- 玩家输入不是主角台词或行动，而是讲给双导演听的创作指令，例如希望某个角色如何表现、下一步向什么方向发展或希望关注什么镜头。
- 玩家指令作为隐藏的 `PlayerDirection` 保存，不直接成为世界事实，也不向场内角色暴露。
- SceneDirector 必须把指令转换为持久的 `PendingDirection`，记录目标、核心意图、前置条件、最早合理时机、完成进度和状态；不能因一次调度未实现就遗忘。
- 如果目标暂时不符合既有事实、角色知识或因果条件，应先安排合理铺垫，在最早合法时机兑现，而不是让角色突然失真。
- 角色仍负责生成具体语言和动作，但本次行为受到 `PendingDirection` 的明确表演约束；只有完成、玩家撤销或因已提交事实变得不可能后才能关闭。无法实现时必须标记 `Blocked` 并在非叙事系统状态中说明原因，不能静默拒绝。
- 当检测到玩家正在输入或使用 STT 时，启用输入慢动作倍率；导演指令编写期间世界仍然推进，但当前场景和离屏世界都会同步变慢。
- 玩家采用全知观看视角；SceneDirector 可以安排离屏镜头，由开普勒完成可见转场。
- 玩家看见离屏事件不等于角色知道这些事件，角色知识权限始终独立计算。

### 3.2 RP 模式

- 禁用主角 Agent，系统不得替主角生成台词、心理、决定或主动行为。
- NPC 可以主动询问、呼唤、等待、催促、离开，也可以让合理的外部事件对主角产生后果。
- 离屏世界继续模拟，但不直接向玩家展示；只有通过目击、传闻、调查、通信等合理渠道才能进入玩家可见上下文。
- 键盘输入、输入框非空或 STT 录音期间启用输入慢动作倍率，降低全局逻辑时间流速，为玩家组织输入提供真实的世界时间余量。
- 未提交文本和 STT 中间结果只能作为调度控制信号，绝不能提供给任何 Agent。

## 4. 连续事件模型

用追加式 `TimelineEvent` 取代 `GameRound` 作为叙事事实源。建议事件至少包含：

- `event_id`、`branch_id`、`parent_event_id`
- `sequence`：分支内单调递增的提交序号
- 现实创建时间与逻辑世界时间
- `world_epoch`：显式记录事件所属世界纪元，使跨 epoch 可见性谓词可在查询层直接验证，不必扫描历史 `WorldRewindCommitted` 推断
- `scene_id`、行为者、目标角色
- 事件类型与结构化内容
- `direct_observers` 与 `potential_learners`：第 5.7 节定义的可观察性集合，是所有可见范围判定的唯一来源
- `observability_computed`：标记本事件已完成确定性可观察性计算，用于区分“空集”与“尚未计算”
- `visibility_scope`：由上述集合推导的投影级可见权限，供记忆、检索和玩家视角过滤使用
- `revealed_event_cursors`：前台可见事件在此记录本次向玩家揭示的源事件游标，是 `reveal_queue` 完成状态的唯一事实来源
- `Draft`、`Committed`、`Interrupted`、`Superseded` 等状态
- 触发原因、因果父事件和对应世界状态变更
- 规范化 `StateChangeSet`：命令 ID、目标投影、实体 ID、操作类型、预期版本、字段变化和验证结果
- 节拍、强度、信息量和叙事目的等节奏元数据

事件分为三类：

1. **可见事件**：RP 模式的玩家输入、角色语言与动作、开普勒叙述、原样展示的导入问候。
2. **隐藏世界事件**：剧场模式的 `PlayerDirection`/`PendingDirection`、离屏行为、Director 计划、规则结果、世界状态变化、章节状态变化。
3. **运行控制事件**：暂停、恢复、输入活动、STT 状态、生成取消；这些事件不属于剧情事实，不进入角色提示词。

所有数据库投影、角色记忆和编年史都由已提交事件派生。重生成和 swipe 不覆盖旧事实，而是从目标事件创建新分支。

事件状态的生命周期必须确定：`Draft` 只用于正在生成的内容，不参与任何投影、记忆和检索；重放和投影重建只处理 `Committed`、`Interrupted` 和 `Superseded`。立即停止或生成失败时，已经向玩家显示过前缀的 `Draft` 转为 `Interrupted`，完全没有显示内容的 `Draft` 直接丢弃，不留下半成品事实。

`sequence` 只在事件真正提交时由单写者分配。`Draft` 不占用序号，因此丢弃草稿不会产生序号空洞，也不需要删除已追加的行。转为 `Interrupted` 的内容按提交时刻取号，与其他已提交事件共享同一条严格递增序列。

### 4.1 状态变更与确定性重放

LLM、规则系统和 Form Agent 只能提出结构化状态变更草案。系统在提交事件前完成 Schema、权限、版本、数值范围和世界规则校验，并将最终生效的规范化 `StateChangeSet` 作为事件负载持久化。

- 投影器只执行事件中已经记录的命令，不解析叙事文本推断状态。
- 重放、分支恢复和启动恢复期间禁止调用任何 LLM，也禁止重新生成 SQL 或状态命令。
- 每条状态命令使用稳定 `command_id`，并通过目标实体版本实现幂等和乐观并发检查。
- 一个事件及其 `StateChangeSet` 在同一数据库事务中提交；投影失败时事件不能部分生效。
- `pacing_state_cache` 和 `reveal_queue` 是可从事件流重算的派生物，不参与该事务；它们只记录对应事件游标，损坏或落后时重算而不是阻塞提交。
- `reveal_queue` 的“已揭示”状态只能由前台可见事件的 `revealed_event_cursors` 重建。前台事件必须记录本次揭示了哪些隐藏事件游标，否则重启或缓存重算后无法区分已呈现与待呈现，会造成重复揭示或漏掉强制揭示标记。
- Form Agent 不得直接执行自由 SQL。旧的自由 SQL 路径必须在事件事实源启用前移除，而不是作为长期优化保留。

### 4.2 三种时间线操作

以下操作语义必须分离：

1. **Branch**：用于 swipe、重生成和用户主动分支。新 `branch_id` 引用父分支与父事件游标，从该游标的完整投影开始独立发展，不修改父分支。
2. **WorldRewind**：用于死亡回归。在同一分支追加 `WorldRewindCommitted`，记录目标 SavePoint、`rollback_scope`、`memory_retainers` 和新的 `world_epoch`。投影器将世界投影和非保留者记忆恢复到书签状态；只有世界规则明确指定的回归者保留循环记忆。系统审计编年史可以保留完整历史，但不得自动进入非回归者、开普勒或 RP 玩家上下文。历史事件仍保留，不能伪装成从未存在。
3. **SavePoint**：不可变的事件游标书签，可附带用于加速恢复的投影 checkpoint，但它本身不创造世界事实。

长会话定期生成投影 checkpoint。恢复时从最近 checkpoint 加载后继续重放，不要求每次从事件 1 开始；checkpoint 只是缓存，删除后仍能从事件流恢复正确结果。

### 4.3 世界模拟与戏剧呈现双循环

连续世界必须把“实际发生什么”和“玩家现在看见什么”分开：

1. **World Simulation Loop**：依据角色目标、日程、位置、关系、世界规则和到期事件持续提交世界事实。该循环去主角中心，不因当前镜头进入呼吸期而停止世界运转。
2. **Presentation Loop**：依据玩家当前可见内容、节奏状态、信息权限和镜头纪律，决定哪些已发生事件或具备因果条件的行动机会现在进入前台。

SceneDirector 主要控制前台事件的频率、顺序、揭示时机和镜头，不得为了制造节奏而临时提高事件严重程度、削弱角色能力、删除已经发生的事件或凭空制造灾难。世界循环可以继续产生离屏事实，呈现循环可以延迟揭示，但不能改写事实。

剧场模式中，重要离屏事实提交后进入 `RevealQueue`，记录发生时间、重要度、相关故事线、允许揭示范围、与当前镜头的因果距离和最迟合理揭示条件。Director 可以等待合适节拍再切镜，也可以合并相关事件后统一展示；如果离屏事件已经对当前镜头产生可观察后果，InformationGate 必须立即开放对应事实，开普勒不得继续假装事件尚未发生。

## 5. 双导演协议

### 5.1 权限边界

双导演不是两个 Agent 共同自由写作，否则会产生抢权、重复叙述和节奏震荡。二者通过结构化节拍契约协作：

| 职责 | SceneDirector（隐藏导演） | 开普勒（显示导演） |
|---|---|---|
| 剧情层级 | 宏观顺序与机会 | 微观呈现与感受 |
| 世界时间 | 提议推进量和事件时机 | 用自然叙述表现时间变化 |
| 镜头 | 决定是否需要切换及候选场景 | 决定具体转场文字和镜头落点 |
| 角色 | 选择获得行动机会的候选角色 | 不替角色生成语言或动作 |
| 信息 | 决定哪些事实现在允许揭示 | 决定信息的叙述顺序、篇幅与强调 |
| 节奏 | 控制节拍序列和整体压力曲线 | 控制句段长度、停顿、环境细节和转场速度 |
| 章节 | 维护章节状态和可能方向 | 只表现已经成立的变化，不提前剧透 |

世界事实与创作约束的优先级固定为：

`已提交事件 > 世界规则与角色知识边界 > PlayerDirection > 角色自主目标 > Director 计划 > 章节预案`

SceneDirector 可以用 `PlayerDirection` 对下一步表演施加明确约束，但不能让角色知道其本来不知道的信息，也不能改写已提交事实。没有玩家指令时，如果角色作出了 Director 未预期但符合人设和世界规则的选择，系统应接受该选择并重新规划，不能重生成角色来强迫剧情服从章节预案。

### 5.2 BeatPlan：隐藏导演给显示导演的契约

SceneDirector 不输出正文，只输出可校验的 `BeatPlan`。建议字段包括：

- `beat_kind`：建立、发展、升级、转折、兑现、余波、呼吸、转场。
- `story_purpose`：本事件为何值得发生，例如推进目标、改变关系、揭示信息或制造选择。
- `target_intensity`：目标压力区间，而不是精确分数。
- `candidate_scenes` 与 `camera_action`：保持、拉远、切换、并行镜头或回到主场景。
- `actor_opportunities`：哪些角色有合理行动机会，并包含 `actor_id`、`reason_to_act`、抽象 `motivation`、角色已知约束、允许目标和紧迫度；不得规定台词、具体动作或泄露剧本结果。
- `time_advance_range`：允许推进的逻辑世界时间范围。
- `reveal_allowlist`：本节拍允许公开的事实。
- `continuity_constraints`：必须保持的地点、在场角色、因果和知识边界。
- `narration_need`：无需开普勒、简短环境反馈、完整场景叙述或正式转场。
- `response_window_hint`：Director 对介入窗口的建议；最终等待与时间倍率由确定性调度器结合输入活动和 `player_load` 决定。等待时长的合成规则是取较大值，即实际窗口不小于 `response_window_hint`，也不小于输入慢动作与 `player_load` 推导出的窗口，两者不相乘。

开普勒只能把 `BeatPlan` 转化为前台叙述，不能修改已提交事实、替角色作决定，或把隐藏章节信息直接说给玩家。

SceneDirector 不能从空白开始自由生成 `BeatPlan`。调度器必须先构建合法候选池，来源包括到期角色活动、角色目标、未完成承诺与冲突、已提交离屏事件、满足前置条件的章节机会和 `PlayerDirection`。候选项先经过位置、知识、因果、冷却和可见性过滤，Director 只在合法候选中选择、排序或组合。

上述字段是完整目标契约。P3 首版只要求 `beat_kind`、`actor_opportunities`、`camera_action`、`narration_need`、`reveal_allowlist` 和 `time_advance_range`；其余字段先由确定性组件计算或留空，只有实验表明必要时才进入运行时必填 Schema。

### 5.3 共同把握节奏的方法

节奏不能只用一个“快慢”分数，因为拖沓和过山车来自不同原因。系统维护共享的 `PacingState`：

- `tension_band`：当前压力档位。
- `information_density`：最近事件引入的新信息量。
- `consequence_density`：最近事件产生的实际状态或关系变化。
- `unresolved_hooks`：尚未回应的问题、威胁、承诺和选择及其年龄。
- `breathing_debt`：连续高压后累积的喘息需求。
- `progress_debt`：连续低变化后累积的推进需求。
- `camera_age`：当前镜头已持续的事件数和逻辑时间。
- `speaker_run`：同一角色连续占据前台的程度。
- `player_load`：消息堆积、未读内容和玩家输入活动。

每次事件完成后，节奏控制器根据结构化事件元数据更新这些值，SceneDirector 再据此选择下一个节拍。开普勒看到目标节拍和约束，但只负责表现层，不重新决定宏观曲线。

#### 5.3.1 确定性节奏特征提取

节奏状态不能由 SceneDirector 自行主观打分。每个已提交事件必须携带可校验的结构化标签，例如威胁、伤害、不可逆后果、新信息、关系变化、目标进展、悬念开启/兑现、镜头切换和玩家负荷。`PacingFeatureExtractor` 使用固定更新函数和时间/事件窗口完成以下工作：

- 根据事件类型和强度更新各项密度、债务、悬念年龄和连续计数。
- 对旧事件贡献进行确定性衰减，避免历史高潮永久抬高当前压力。
- 将多维 `PacingState` 投影为只服务于前台呈现的 `visible_intensity`。
- 独立计算 `world_urgency`，表示世界事件在因果上是否必须及时发生；它不因前台进入呼吸期而归零。

节奏标签直接从事件类型、规范化 `StateChangeSet`、StoryThread 变化和镜头操作推导，不增加一次 LLM 标签调用。节奏状态的累计、衰减和转移全部由普通代码完成。

#### 5.3.2 四阶段节奏控制器

`PaceGovernor` 使用带迟滞的四阶段状态机控制前台节奏：

| 阶段 | 作用 | 允许行为 |
|---|---|---|
| `Build` | 逐步建立压力与期待 | 增加阻力、信息、关系张力或选择，但不得无铺垫跳级 |
| `Sustain` | 让当前矛盾充分成立 | 维持当前强度并让角色处理核心事件，不继续叠加更大危机 |
| `Fade` | 让高压自然结束 | 停止引入新升级，结算仍在发生的危险、决定和后果 |
| `Relax` | 提供恢复与角色生活空间 | 日常活动、关系反馈、余波、信息整理和低压目标推进 |

状态转移依据 `visible_intensity`、即时未解决威胁、`progress_debt`、`breathing_debt` 和玩家负荷执行。高低阈值必须分离，并要求满足最短驻留或连续事件条件，避免单个事件造成 `Build` 与 `Relax` 之间来回震荡。不可避免的新因果事件可以中断 `Fade` 或 `Relax`，但必须记录触发来源，不能由 Director 以“需要刺激”为由自行制造。

四阶段只控制呈现频率和允许的节拍类型，不修改角色能力、规则难度或已经排定世界事件的严重程度。具体阈值、窗口和衰减系数由第 14 节实验校准，不在设计阶段凭直觉写死。

### 5.4 防拖沓规则

出现以下情况时累积 `progress_debt`：

- 多个连续事件没有产生新信息、目标变化、关系变化、状态变化或有效选择。
- 同一问题被不同角色反复表达，却没有新的行动。
- 环境描写重复已经明确的事实。
- 角色只是轮流回应主角，没有自己的议题和相互作用。
- 当前镜头已经失去待解决事项，却仍因惯性停留。

当 `progress_debt` 达到阈值时，Director 必须选择一种有因果依据的推进方式：兑现已有伏笔、让角色完成计划、引入新信息、制造决定、推进时间或切到更有事件价值的镜头。禁止凭空制造灾难来伪装推进。

### 5.5 防过山车规则

出现以下情况时累积 `breathing_debt`：

- 连续多个节拍都提升压力，没有余波或消化空间。
- 镜头在短时间内频繁切换。
- 新角色、新威胁和新秘密连续引入，却没有完成任何已有线索。
- 情绪、战斗、谜团和章节转折同时争夺注意力。
- 重大事件之后立刻出现更大的事件，导致前一事件失去重量。

高 `breathing_debt` 时，Director 应优先选择余波、安静互动、关系反应、日常活动或信息整理。呼吸节拍仍应包含角色意图和细微变化，不能退化为无意义闲聊。

### 5.6 镜头纪律

- 镜头切换必须服务于信息、因果、悬念或角色弧线，不能只为制造“电影感”。
- 除非发生即时危险或玩家主动切入，镜头应满足最短停留，避免一句一切。
- 同一镜头长期没有目标、冲突、关系变化或待解决事项时，应结束、跳时或切换。
- 剧场模式允许离屏镜头；RP 模式的离屏事件只能作为隐藏事实提交。
- 开普勒负责让转场可理解，明确地点、时间和视角变化，但不解释后台导演意图。

### 5.7 信息门与世界可行性校验

SceneDirector 可以读取完整章节规划，但不能直接把完整剧本传给开普勒或 Character Agent。系统新增两道硬门：

1. **InformationGate**：根据已提交事件、观察渠道、角色知识和模式权限维护可揭示事实集。`reveal_allowlist` 只能引用该集合；给 Character Agent 的 `ActorBrief` 只能包含该角色可知事实和不泄露结果的抽象动机。
2. **WorldAffordanceValidator**：验证候选场景是否存在、角色位置是否可达、目标是否存在、知识来源是否合法、前置条件是否成立，以及事件是否与当前世界状态冲突。

每个世界事件提交时必须计算并持久化两个互不混用的可观察性集合：

- `direct_observers`：事件发生的同时即可感知的主体，依据同场景在场关系、位置与距离、感官能力、遮挡和实时通信链路计算。
- `potential_learners`：之后可能通过传闻、调查、通信、痕迹或第三方转述得知的主体，每项必须带获知渠道和最早获知条件，不代表现在已经知道。

两者都不得由 LLM 自由填写，必须由 WorldAffordanceValidator 在提交时确定性计算。角色记忆的 `visibility_scope` 只能引用 `direct_observers` 或已经成立的 `potential_learners` 渠道，不能引用尚未满足条件的未来获知。

InformationGate 将玩家视角作为一等主体：剧场模式使用全知 `player_view`，RP 模式使用主角当前知识与观察渠道。

当 `direct_observers` 命中当前镜头主体，或事件已在当前镜头内留下可感知的物理痕迹时，RevealQueue 将该事件标记为 `must_reveal_before_next_foreground_event`。这是“可观察后果必须立即呈现”的确定性定义。仅出现在 `potential_learners` 中的事件不触发强制揭示，可以按节奏延迟到合理渠道成立时再展示。

SceneDirector 提出的新地点、角色或世界事件必须先转化为带前置条件的世界候选，经校验并提交后才能进入镜头。不能为了回应玩家或完成章节目标直接叙述一个尚不存在的场景。

`BeatPlan` 提交前必须检查候选角色位置与冷却、角色知识权限、揭示事实来源、时间推进上限、镜头合法性和连续性约束。校验失败时允许有限重试；仍失败则使用不引入新事实的最小安全计划，例如继续当前合法活动、短暂等待或推进到下一个已确定事件。

### 5.8 有限剧情反思与计划修订

章节规划不采用“完成一条固定 plot chain 后强制切场”的模式，而是维护多个并行 `StoryThread` 和带前置条件的未来机会。反思只修改尚未发生的未来计划，不能修改已提交事实。

反思由实际事件触发，而不是机械地每隔固定回合运行。触发来源包括：新的 `PlayerDirection`、现有计划前置条件失效、章节里程碑成立、节奏债务越过阈值、故事线长期停滞或当前计划没有合法机会。

普通反思每次最多新增、删除或调整一个未完成 `StoryThread`/剧情机会，并记录 `plan_version`、修改原因和修改范围，防止 Director 连续推翻自己的规划。`PlayerDirection` 和事实冲突修复可以获得更高修订权限，但仍必须通过 InformationGate 与 WorldAffordanceValidator。

`DirectionDecomposer` 专门负责把抽象 `PendingDirection` 分解为可验证的前置条件链，例如移动、信息传递、关系铺垫、日程调整、道具条件或事件到期。每个铺垫候选都必须有目标状态、前置条件、完成判据和来源指令，并逐项通过 WorldAffordanceValidator。只有具体缺失条件无法通过任何合法行为满足时才能标记 `Blocked`，且必须向非叙事 UI 返回缺失条件列表。

### 5.9 两级 SceneDirector

两级 Director 是同一个隐藏导演的两种运行档位，共享世界事实、`PacingState`、`BeatPlan`、`PendingDirection` 和 `RevealQueue`，但使用不同的上下文预算、输出权限和模型配置。

#### DirectorPulse：事件级轻量判断

每个前台可见事件和被 WorldTickGovernor 确定性提升的重要离屏事件提交后运行一次，只读取最近事件增量、当前节奏阶段、现有计划、合法候选摘要和待揭示摘要。普通离屏日程、规则快进和被合并的低价值事件不调用 Pulse。它只能输出：

- 继续当前计划或请求完整重规划
- 从当前计划允许的合法候选中选择下一机会
- `narration_need`、响应窗口和计划范围内的镜头/揭示建议
- `replan_reason` 与紧迫度

DirectorPulse 不得修改 `PacingState`、新增/删除 `StoryThread`、改变 `BeatPlan` 的核心目的、扩大 `reveal_allowlist` 或创造新世界事实。输出校验失败时由确定性调度器根据当前计划和候选优先级降级选择，不能阻塞世界。

Pulse 设定严格延迟期限。超时后调度器立即使用确定性降级选择；迟到输出只有在事件游标和 `plan_version` 仍匹配时才能影响下一次机会，不能追回并改写已经开始或提交的事件。

#### DirectorReflection：关键节点完整规划

只有以下情况调用完整 Director：首次建立计划、新 `PlayerDirection`、四阶段发生转换、当前计划失效或耗尽、Pulse 提出的重规划理由通过规则确认、章节里程碑成立、节奏债务越界、RevealQueue 出现必须处理的因果冲突，或连续校验失败。

DirectorReflection 可以生成新 `BeatPlan`、执行第 5.8 节的有限故事线反思、分解 `PendingDirection` 的因果铺垫，并调整镜头策略。它仍不能修改已提交事实，也必须通过 InformationGate、WorldAffordanceValidator 和计划修订预算。

两级 Director 分别配置模型、上下文和 token 预算。Pulse 优先低延迟、稳定结构输出；Reflection 优先长程理解和规划质量。两者的日志必须记录输入事件游标、计划版本、事件范围、触发原因、输出和降级路径，便于比较脉冲调用带来的实际收益。

### 5.10 单次调度流程

1. 调度器读取逻辑时间、到期活动、角色目标和待处理事件。
2. 节奏控制器更新 `PacingState` 并识别推进债务、呼吸债务和玩家负荷。
3. 候选构建器生成合法候选池，并读取上一事件提交后 Pulse 已经就绪的机会建议；没有就绪结果时按确定性优先级选择，不等待 Pulse 返回。
4. 达到完整触发条件时运行 DirectorReflection，生成或修订 `BeatPlan` 与 Actor motivation；否则沿用当前计划。
5. 若需要环境、时间或镜头变化，开普勒先输出相应叙述。
6. 被选中的 Character Agent 根据自身知识、目标和 motivation 自主决定具体行为。
7. 规则系统处理必要判定，事件通过一致性验证后提交。
8. 更新世界投影、相关角色记忆、编年史、PendingDirection、RevealQueue 和下一唤醒时间。
9. 提交完成后触发本事件对应的 Pulse 钩子，其结果只用于下一次机会选择。
10. 回到事件级评估，而不是执行预先排好的固定角色队列。

Pulse 是提交后钩子，不是调度链上的同步阻塞环节。它的建议永远作用于下一次机会，因此角色连续对话不会因为每事件判断而出现额外停顿。

开普勒不必在每条角色消息之间发言。`narration_need = none` 时角色可以自然连续交流，避免旁白打断对话和增加拖沓感。

## 6. 角色自治与去主角中心

每个角色维护独立的 `CharacterAgencyState`：

- 长期目标、当前目标及优先级
- 当前活动、日程、位置和预计完成时间
- 生理需求、情绪动量和风险状态
- 未完成的问题、承诺、冲突和关系事项
- 对其他角色的关系与当前注意目标
- 自己观察到的事实、推断和错误认知
- 行动冷却、上次主动时间和下一次唤醒时间

角色主动行为必须来源于这些状态，而不是随机发言概率。角色可以主动与其他 NPC 交流，也可以询问、呼唤或寻找主角；但不能因为主角是玩家就天然获得最高调度优先级。

SceneDirector 负责分配“行动机会”，Character Agent 负责决定具体行为。没有 `PendingDirection` 时，如果角色选择沉默、拒绝普通导演计划或改变目标，只要符合其知识与性格，系统就应提交结果并重新规划。存在已经通过因果与知识校验的 `PendingDirection` 时，Character Agent 仍可自由决定具体表演，但输出必须满足该指令的核心约束；不满足的输出视为契约失败而非角色对玩家导演权的否决。

## 7. 离屏世界的分层模拟

为了同时保持世界活性和控制 LLM 成本，采用分层精度：

1. **当前镜头**：完整 Character Agent 推演和可见事件。
2. **同区域离屏角色**：按目标、日程和关系生成简化事件，重要事件才调用 LLM。
3. **远方角色**：主要通过日程、规则和重大事件更新进行粗粒度模拟。
4. **精度提升**：角色进入镜头或事件变得重要时，根据已提交离屏事件恢复详细状态，禁止重新编造过去。

离屏事件只写入实际参与者的记忆。RP 模式下玩家不可见；剧场模式下只有 Director 决定切镜并由开普勒完成叙述后，事件才进入全知观看流。

### 7.1 WorldTickGovernor 与背压

世界循环由独立的 `WorldTickGovernor` 控制资源和积压，但不能以玩家是否输入作为世界是否存在的开关：

- 分别限制每个逻辑时间窗口内当前场景、同区域和远方模拟的事件数、并发数、LLM 调用数与 token 预算。
- `RevealQueue` 设置容量、故事线配额和合并策略。超限时只合并前台展示项，不删除已提交世界事实；多个低价值变化压缩为引用原始事件游标的摘要展示项。
- 事件涉及当前镜头主体、`PendingDirection`、不可逆状态变化、StoryThread 里程碑或可观察通信链路时，确定性提升模拟精度。
- 超出 LLM 预算时，世界循环退化为日程、规则和状态命令模拟；待预算恢复后可提升后续事件精度，但不能重新生成或改写已经提交的粗粒度事实。
- 当生成吞吐落后于逻辑时钟时，按世界时间排序积压任务，合并低优先级任务并限制新 LLM 工作；同时按第 8.1 节计算 `generation_backpressure_factor`，降低世界时钟实际推进速度，但不修改用户设定的 `session_time_scale`。

预算、积压和降级状态必须持久化并进入诊断指标，防止应用重启后绕过配额。

## 8. 世界时间、暂停与输入活动

世界时间与现实单调时钟强绑定：

`world_delta = monotonic_real_delta * session_time_scale * input_slow_factor * generation_backpressure_factor`

`session_time_scale` 由世界速度控件决定；`input_slow_factor` 在正常状态为 `1`，键盘输入或 STT 期间降为大于 `0` 且小于 `1` 的配置值。三个因子共同作用于当前场景、离屏模拟、角色日程、事件到期和世界规则，不能只改变 UI 动画或前台消息间隔。

### 8.1 时间推进与生成背压

除原样展示的 `ImportedGreeting` 外，前台可见事件必须逐条经过 LLM，既不能合并也不能规则快进，因此世界时钟不能只按现实时间无条件前进，否则前台展示会永久落后于世界事实。

- `generation_backpressure_factor` 取值范围为 `(0, 1]`，由确定性 Governor 依据前台待生成事件数和前台已落后的世界时间计算，不由任何 Agent 决定。
- 该因子是系统可观测的背压量，与用户设定分离。UI 必须能显示当前实际时间倍率与背压状态，不得让玩家以为自己的设置被忽略。
- 前台无积压时该因子为 `1`；积压增大时单调下降，并设有下界，避免世界完全停滞。
- `session_time_scale` 需要设置上限，由实测前台吞吐推导，作为第 14.2 节的实验参数校准。
- 世界时钟在会话运行且未暂停时按上述公式连续前进。
- 事件在调度时记录 `scheduled_world_time`；LLM 生成延迟不改变该事件在世界中的发生时间。
- 所有生成结果通过事件单写者按调度序号提交，技术延迟不能造成事件因果倒序。
- 当生成速度落后于世界时钟时，WorldTickGovernor 先降低离屏模拟精度、合并低价值变化并使用规则快进；仍不足时才通过 `generation_backpressure_factor` 减慢世界时钟。
- 背压因子、当前积压和落后世界时间必须持久化并进入诊断指标。

### 8.2 暂停

- 暂停后不启动新事件、不推进逻辑时间、不运行离屏模拟。
- 默认让当前原子事件完成后暂停；另提供立即停止，用于取消正在生成的内容。
- 应用关闭时默认自动暂停。若未来需要真正离线推进，应作为独立后台服务另行设计。

### 8.3 输入与 STT

- `InputActivityStarted`、`InputActivityEnded`、`SttStarted` 和 `SttEnded` 是运行控制事件。
- RP 模式和剧场模式检测到输入活动后都立即更新 `input_slow_factor`，不冻结单独的前台循环，也不让离屏世界继续以原速运行。
- 慢动作期间世界仍按因果推进并提交事实；只是新的逻辑时间到期速度整体下降。
- 输入活动期间前台进入受限准入：只放行 `must_reveal_before_next_foreground_event` 事件和无法回避的直接因果后果，其余已提交事实先进入 `RevealQueue`，等玩家提交或结束输入后再择机揭示。世界不停止，被限制的只是呈现。
- 受限准入不得用于隐藏玩家当前镜头已经能直接观察到的后果，也不得让角色对已发生事实表现得一无所知。
- “输入期间新增前台事件数上限”是第 14.2 节的实验参数，需要在 P4 校准后固定。
- 剧场模式提交后把内容记录为 `PlayerDirection`，交由 SceneDirector 更新 `BeatPlan`。
- 未提交文字、STT 临时转写和按键内容不得写入时间线、日志提示词或 Agent 上下文。
- 玩家提交后立即取消尚未执行的旧调度计划。RP 输入作为新世界事实重新生成 `BeatPlan`；剧场输入作为新的导演指令重新生成 `BeatPlan`。

### 8.4 插话

- 输入框在自动生成期间始终可用。
- 玩家提交时取消当前流式生成；已经展示的前缀保留为 `Interrupted` 事件，因为玩家可能针对它作出反应。
- 未生成的后缀和旧角色队列全部废弃。
- Director 必须把插话作为最高优先级的新事实重新评估场景。
- `Interrupted` 标记 `partial = true`，其已显示文本属于可观察事实但不得自动续写。记忆投影只能确定记录“角色开口并被打断”及已经完整表达的内容，不能把未完成句推断成完整命题。

### 8.5 取消域、单写者与幂等

连续模式使用三个独立且有明确级联关系的取消域：

1. `world_loop`：控制世界时钟、到期任务和离屏模拟；暂停可挂起，立即停止或会话关闭才取消。
2. `plan`：控制当前 BeatPlan、Director 调用和尚未执行的候选；新玩家输入、事实冲突或分支会取消并重建。
3. `generation`：控制单次开普勒或 Character Agent 流式生成；玩家插话只取消当前 generation，并按需要使所属 plan 失效，不得顺带终止 world_loop。

所有事件通过单写者队列提交。数据库对 `(branch_id, sequence)` 建立唯一约束，事件和状态命令均使用稳定幂等键；重试只能返回既有提交结果或继续未完成事务，不能产生重复事件。后台模拟、Director 和前台生成不得绕过单写者直接修改投影。

## 9. 提示词和 Agent 职责迁移

### 9.1 开普勒

- 将 `GmAgent` 重命名并收窄为可见叙述者。
- 删除 GM、裁判、回合开场和世界总控制者语义。
- 只描述客观环境、时间变化、非角色世界事件、镜头与场景转移。
- 不复述角色已经输出的动作，不替任何角色说话或描述其内心。

### 9.2 SceneDirector

- 合并原 NPC 位号调度和章节切换的宏观职责，但不沿用固定顺序。
- 可查看章节规划、全局世界状态、所有场景和隐藏事件。
- 只输出结构化 `BeatPlan` 与章节状态建议，不输出玩家可见正文。
- 章节切换必须依据已经发生的事件和条件，不得仅因节奏需求改写事实。
- 内置世界书不再用单一 `current_chapter` 数字直接切换人物、地点和局势事实。现有章节变体条件迁移为基于已提交世界状态、StoryThread 里程碑和事实谓词的条件条目；章节号只保留为软叙事标签和检索辅助，不能覆盖投影事实。

### 9.3 Character Agent

- 保留现有角色人设、语言风格、私有记忆和受限数据库视角。
- 输入改为当前场景事件、个人知识、目标、关系和 Director 提供的“行动机会”。
- 输出仍只包含自己的语言、动作和必要的主观感受。
- 删除 `GameRound`、GM 开场、位号和固定前序角色依赖。

### 9.4 RuleResolver 与状态提交

- 将骰子和战斗判定从开普勒身份中拆出，作为不可见规则服务。
- Form Agent 读取待提交事件草案，输出结构化 `StateChangeSet` 提案，不再等待“回合结算”，也不得直接执行 SQL。
- 状态命令经校验后与叙事事件原子提交；Form Agent 的提案本身不是事实，只有事件中记录的最终命令可以更新投影。
- 重要事实及时提交；低优先级记忆和摘要可以批处理，但必须保持事件游标和幂等性。
- 重放期间不运行 Form Agent，只执行事件中已经持久化的命令。

## 10. 数据与存档迁移

新增或等价实现以下持久化对象：

- `timeline_events`
- `timeline_branches`
- `save_points`：不可变事件游标书签，与可删除的 `projection_checkpoints` 分开存储
- `projection_checkpoints` 与投影游标
- `scene_states`
- `character_agency_states`
- `story_threads`
- `pending_directions`
- `reveal_queue`
- `director_plan_versions`
- `pacing_state_cache`
- `world_scheduler_jobs`、预算窗口与积压状态
- `memory_embeddings`
- `lorebook_sources` 与 `character_card_sources`
- `lorebook_condition_entries`：世界书条目的事实谓词条件载体，承接现有章节变体资产

`ChatSession` 增加不可变 `game_mode`，并持久化 `session_time_scale`、`input_slow_factor`、世界时钟锚点和暂停状态。`pacing_state_cache` 可从事件流重算，落库只用于恢复性能，并必须记录对应事件游标。

现有全局状态、人物、地图、物品等业务表作为事件投影保留。`RoundPhase`、`GameRound`、`RoundVariantSet` 和会话中的 round snapshot 字段不属于投影，在 P1 一次性删除，不运行新旧编排双轨。删除动作必须排在 `lorebook_condition_entries` 落地并成功导入 P0 章节条件资产之后，不得只依据本节就先行删除变体类型。

`character_memory` 在 P1 同步重建：删除 `round_index`，增加 `owner_character_id`、`source_event_id`、`world_time`、`world_epoch`、`observation_channel`、`confidence`、`visibility_scope`、记忆类型和回归保留标记。记忆写入必须引用已提交事件；WorldRewind 按 `memory_retainers` 重新投影可访问记忆；向量索引与记忆检索的可见谓词为 `(owner_character_id, world_epoch) ∈ 可见集`，其中可见集等于当前 epoch，加上该角色属于 `memory_retainers` 时的历史 epoch。单一 epoch 过滤不成立：它会屏蔽回归者的循环记忆，或放行非回归者的旧 epoch 记忆。WorldRewind 后被回滚记忆的 embedding 保留但标记 epoch，只能通过上述谓词访问，不删除也不改写。

当前版本尚未发布，因此不实现旧游戏存档迁移、只读归档或回合到事件的转换。检测到连续模式之前的游戏存档 schema 时，直接删除旧会话、回合、变体、存档点、历史游戏状态和相关快照，再初始化新的事件时间线。Agent API 配置、路由、用户设置和已导入资源不属于游戏存档，除非字段不兼容，否则必须保留。

## 11. 向量化与记忆权限

混合检索由三层组成：

1. 核心身份、性格约束、当前状态和强制世界书条目固定注入。
2. SillyTavern 关键词、常量、选择性、递归、顺序、预算等规则确定性激活。
3. 大型背景、关系历史和长期记忆通过向量相似度召回。

人物核心人设不得只依赖向量召回，否则一次漏召回就会改变角色。向量在导入或内容变化时后台生成，使用内容哈希避免重复计算。

角色记忆必须带 `owner_character_id`、来源事件、世界时间、`world_epoch`、观察渠道、可信度和可见权限。Character Agent 只能检索自己的记忆和当前可观察事实；Director 可以读取全局事实；开普勒可读取全局事实，但 RP 模式输出前必须经过玩家可见性过滤。

跨 epoch 检索必须使用第 10 节的 `(owner_character_id, world_epoch)` 可见集谓词。非回归者不得召回被回滚 epoch 的记忆，回归者必须能召回自己的循环记忆；该约束适用于向量召回、关键词激活和固定注入三条路径，不能只在其中一条实现。

## 12. SillyTavern 兼容范围

### 12.1 需要支持

- Character Card V1/V2/V3 的 JSON、PNG 和 CHARX 导入。
- 核心角色字段、首条消息、示例对话、系统提示词、历史后置指令和备用问候。
- 内嵌 `character_book` 与独立 lorebook。
- 关键词、次关键词、常量、选择性、大小写、正则、递归、插入顺序、位置、概率和 token 预算等核心运行语义。
- 未识别的 `extensions` 字段原样保存并在导出时保留。

### 12.2 明确排除

- STscript、Quick Reply、Automation ID 等插件自动化。
- 第三方 TTS、表情、Live2D、脚本执行和扩展运行时。
- 与 SillyTavern Vector Storage 插件的内部实现兼容；本项目提供自己的原生向量索引。

角色卡 `first_mes` 及用户选中的备用问候必须原样展示，不拆分、不改写，也不交给开普勒重新演绎。系统将其记录为带卡片来源和版本信息的 `ImportedGreeting` 可见事件，并标记为会话开始前的权威 `authored_prologue`。

导入流程必须从角色卡、关联世界书和 `ImportedGreeting` 提取 `InitialSceneProposal`，尽可能包含地点、在场角色、逻辑时间、已声明关系和序章中明确成立的状态。该提案通过 WorldAffordanceValidator 后作为会话初始投影提交。

大量真实角色卡的 `first_mes` 只有一段没有地点和时间的寒暄，因此字段缺省不能阻止开始游戏。缺省时使用确定性默认值：地点缺失使用抽象场景 `unspecified_locale`，时间缺失使用世界纪元起点，在场角色缺失使用卡片主角与玩家主角。只有当提案与已导入世界书或已有世界事实存在显式冲突时才拒绝启动会话，并在非叙事 UI 中列出冲突项。缺省场景在后续事件中可以被首个明确地点、时间或在场事实具体化，具体化过程属于补充而不是改写。

序章中对主角既有行为的描述属于卡片作者提供的前史，不视为系统在 RP 模式中代替玩家作出新行动。它可以初始化世界事实，但必须保留 `authored_prologue` 来源，使后续记忆和因果分析能够区分导入前史与运行时玩家行为。后续生成严格遵守双导演与主角控制边界。

## 13. 分阶段实施顺序

### P0：行为规范与固定样本

- 固化本文中的事件、权限、模式和双导演契约。
- 从现有游戏记录建立节奏评估样本，标注拖沓、跳跃、知识泄露和角色失真。
- 直接以连续模式为唯一目标架构，不为尚未发布的六段模式维护兼容开关或长期双轨实现。
- 先把现有 `ChapterVariantRenderer` 中的章节条件和被条件包裹的世界书内容导出为 `lorebook_condition_entries` 资产草案，记录原章节阈值、内容分支和建议事实谓词。该导出必须在 P1 删除任何 round/变体运行时类型之前完成，作者内容资产不得随旧存档一起清理。

### P1：事件事实源

- 引入事件/分支表、逻辑世界时钟、投影游标、checkpoint 和事件单写者；事件显式保存 `world_epoch`，`sequence` 只在提交时分配，Draft 不占序号。
- 移除 Form Agent 自由 SQL，提交并重放规范化 `StateChangeSet`；重放期间禁止调用 LLM。
- 同批重建 `character_memory`，落实所有者、来源事件、观察渠道和权限。
- 实现最小确定性 `direct_observers`：同 `scene_id` 在场角色加事件直接参与者。它是 P2 完整可观察性计算的基础版本，P2 只做扩展而不是另建一套语义。记忆的 `visibility_scope` 必须由该结果推导，禁止写入占位值等待后续回填。
- 实现 Branch、WorldRewind、SavePoint 三种时间线语义以及死亡回归的显式 scope。
- 落地 `lorebook_condition_entries`，导入 P0 的章节条件资产草案，再删除旧 round/变体运行时类型与快照字段。
- 清理旧游戏存档及所有 round 类型/快照字段，保留 API、Agent、用户设置、导入资源和世界书条件资产。
- 出口：固定样本重放得到相同投影；swipe、分支、checkpoint 和死亡回归均符合各自语义；所有角色可感知事件的 `direct_observers` 可从事件确定性重算，非感知类事件（Director 计划、章节状态、运行控制等）显式记录空集并置 `observability_computed`，两类都不允许占位值；章节条件资产完成迁移且无内容丢失。

### P2：无 LLM Director 的确定性连续世界

- 实现动态唤醒、逻辑时间倍率、暂停、输入慢动作、三级取消域和生成抢占。
- 实现合法候选构建器、InformationGate、WorldAffordanceValidator、确定性规则调度器，并把 P1 的最小观察者集合扩展为完整 `direct_observers` 与 `potential_learners`。
- 实现输入活动期间的前台受限准入：只放行强制揭示与不可避免因果事件，其余已提交事实进入 RevealQueue 等待提交后揭示。
- 实现最小 `ActorBrief` 过滤，使 P2 的角色调用已经受知识边界约束；P3 只负责收窄提示词文本。
- 实现 WorldTickGovernor、有界 RevealQueue、预算、背压、速率限制、失败退避和规则降级；所有前台揭示必须写入 `revealed_event_cursors`，缓存重算结果必须与未损坏缓存一致。
- 仅实现当前场景角色自治和 NPC 对 NPC 行为；暂不加入节奏 Agent、远方角色和向量检索。
- 出口：玩家沉默时角色仍活动；知识泄露为零；世界速度真实改变逻辑时间；输入期间前台事件数不超过配置上限；前台世界时间滞后、待生成事件数和 `generation_backpressure_factor` 均处于 P2 校准的通过范围；RevealQueue 从事件流重算后无重复、无遗漏；长时间自动运行成本和积压有界。

### P3：节奏控制与单级完整 Director

- 从 `StateChangeSet` 和事件类型实现 `PacingFeatureExtractor`，首版只持久化最小 PacingState。
- 实现四阶段 PaceGovernor、迟滞、最短驻留、确定性降级与精简 `BeatPlan`。
- 先实现 `DirectorReflection`，将 NPC 调度、场景编排、并行 StoryThread 和有限反思迁入隐藏导演。
- 收窄开普勒与 Character Agent 提示词，Character Agent 只接收过滤后的 `ActorBrief` 和 motivation。
- 出口：建立“无节奏连续基线 vs 确定性节奏 vs 完整 Director”的可重复对照，三个硬门通过。

### P3b：事件级 DirectorPulse

- 在 P3 基线稳定后，为每个前台事件和被提升的重要离屏事件加入 `DirectorPulse`，保持用户确认的两级 Director 最终架构。
- Pulse 首先以影子方式记录建议，不阻塞当前消息；确认收益后再允许影响下一次机会选择。
- 普通离屏日程、规则快进和低价值合并事件不触发 Pulse；重要离屏事件的提升规则由 WorldTickGovernor 的确定性谓词决定。
- Pulse 作为事件提交后的钩子运行，产出“下一次机会建议”；调度流程只消费其已就绪结果，不等待其返回。
- 出口：Pulse 改善节奏或候选选择的收益高于额外延迟与 token 成本，且不增加计划震荡。出口不达标时收紧 Pulse 触发谓词或降低模型档位，不删除该层；影子期设有最长时长上限。

### P4：两种固定会话模式与玩家导演权

- 会话创建时锁定剧场/RP 模式，落实主角 Agent、玩家知识和离屏可见性的不同权限。
- 实现 `PlayerDirection`、`PendingDirection`、DirectionDecomposer、完成/阻塞状态和因果化强制实现。
- 实现 RevealQueue、剧场择机切镜、RP 隐藏离屏事实和可观察后果强制揭示；每次前台展示记录 `revealed_event_cursors`，并用损坏缓存后的完整重算验证揭示结果一致。
- 完成键盘/STT 输入慢动作、玩家插话和 `Interrupted` 事件语义。

### P5：离屏分层扩展

- 先增加同区域简化模拟，再增加远方角色的规则级粗粒度模拟。
- 通过确定性谓词提升重要离屏事件精度，并验证 WorldTickGovernor 的队列合并与成本上限。
- 出口：离屏世界长期运行有界，角色进入镜头时能够从已提交事实恢复而不重写过去。

### P6：记忆检索与向量增强

- 先实现按所有者、可见权限、世界时间和事件来源过滤的确定性检索。
- 在权限过滤稳定后增加人物资料、世界书与长期记忆的向量索引和混合召回。
- 明确单机 SQLite 的向量实现、索引重建、嵌入模型变更和无向量降级路径。

### P7：SillyTavern 兼容

- 首先实现 JSON 与 PNG 角色卡、世界书导入导出、未知扩展保留和 `ImportedGreeting` 初始场景投影。
- 隔离插件执行能力，验证核心世界书激活语义后再加入 CHARX。
- 导入角色生成独立 Agent 配置和自治状态；向量索引由 P6 能力异步增强，不能阻塞基础导入。

### P8：界面与收尾

- 将首页从回合卡片改为连续事件流；新建会话时选择模式，进入会话后不提供模式切换。
- 加入暂停、立即停止、逻辑世界速度、输入慢动作状态、STT 和当前镜头提示。
- 剧场模式以非叙事状态显示导演指令的 `Pending`、`Realizing`、`Completed`、`Blocked` 状态；不把状态文字伪装成角色台词。
- 剧场模式允许显示镜头位置；RP 模式不得暴露隐藏场景数量、人物或事件提示。
- 更新 README、架构说明和兼容性文档，并确认旧六段入口与类型已在 P1 清除。

## 14. 节奏验证实验

节奏效果必须通过对比实验验证，不能只凭提示词“感觉合理”。

### 14.1 基线场景

至少准备以下少量但高价值的固定场景：

- 玩家长时间沉默，在场角色能否自然地彼此交谈和继续活动。
- NPC 向 RP 主角提问，玩家正在慢速输入或使用 STT。
- 长时间平静日常，系统能否推进而不凭空制造危机。
- 连续战斗或高压剧情后，系统能否保留余波和情绪重量。
- 多个离屏事件同时到期时，Director 能否选择合适镜头而不频繁跳切。
- 剧场模式离屏镜头与 RP 模式知识隔离。
- 玩家在角色流式发言中插话。
- 章节条件满足、偏离或延迟满足时的 Director 行为。

### 14.2 量化指标

- **停滞长度**：连续多少可见事件没有信息、目标、关系或状态变化。
- **压力波动**：强度档位变化幅度及短周期反转次数。
- **高压连续长度**：没有余波节拍的连续高压事件数。
- **镜头切换率**：单位事件内的镜头切换次数及无因果切换比例。
- **角色垄断度**：同一角色连续占据前台的比例。
- **主动性覆盖**：非主角触发事件、NPC 对 NPC 互动和角色自主目标事件的比例。
- **玩家负荷**：玩家未读事件积压、输入期间发生的新前台事件数。
- **前台滞后**：前台已展示事件与当前世界时钟的世界时间差、待生成前台事件数和 `generation_backpressure_factor` 分布。
- **知识泄露率**：角色或 RP 玩家看到不具备合理来源的信息的次数。
- **调用成本**：每个有意义状态变化所需的 LLM 调用数与 token 数。
- **两级调用比**：Pulse 与 Reflection 的调用量、平均延迟、token 占比，以及 Pulse 请求重规划后被规则接受的比例。
- **指令兑现率**：`PendingDirection` 完成、阻塞、撤销的比例及从提交到合理兑现的世界时间。
- **揭示延迟**：离屏事实从发生到进入镜头的时间，以及已经产生可观察后果却仍未揭示的违规次数。
- **揭示重建一致性**：删除 RevealQueue 缓存后从 `revealed_event_cursors` 重建，与删除前待揭示集合的差异数。
- **可观察性完整性**：`observability_computed = false` 的已提交事件数、角色可感知事件缺少直接观察者的数量，以及非感知事件被伪造观察者的数量。
- **事件序列完整性**：缺少 `world_epoch` 的已提交事件数、持有 `sequence` 的 Draft 数和分支内重复序号数。
- **状态震荡率**：四阶段在短窗口内反向切换或违反最短驻留的次数。
- **非法候选率**：Director 选择后被位置、知识、因果或可见性规则拒绝的比例。
- **计划修订幅度**：单次反思修改的故事线数量及无事实触发的修订次数。

以下是不可配置的发布硬门：知识泄露次数为 `0`；跨 epoch 记忆泄露给非回归者的次数为 `0`；违反节奏最短驻留/迟滞规则的状态转换次数为 `0`；重复事件、部分投影提交和固定样本重放差异为 `0`；会话内模式变化次数为 `0`；可观察后果越过下一前台事件仍未揭示的次数为 `0`；RevealQueue 重建差异为 `0`；已提交事件未完成可观察性计算的数量为 `0`；持有 `sequence` 的 Draft 数为 `0`；缺少 `world_epoch` 的已提交事件数为 `0`；`PendingDirection` 未经 `Completed`、`Blocked` 或 `Cancelled` 终态即消失的次数为 `0`。

非法候选率、队列峰值、调用预算、节奏阈值、衰减窗口和输入慢动作倍率先作为实验参数。`session_time_scale` 上限和 `generation_backpressure_factor` 下界必须由 P2 的实测前台吞吐推导。P2 结束前必须根据固定样本给这些参数设定首版通过范围，不能带着“以后再调”的空阈值进入 P3。

### 14.3 人工评估

对同一初始状态分别运行旧编排、无节奏控制的连续编排和双导演编排。盲评以下维度：

- 世界是否在玩家沉默时仍然成立。
- 角色是否像在追求自己的目标，而不是等待主角触发。
- 剧情是否有实际推进但不急于制造高潮。
- 高压事件是否有铺垫，重大后果是否有消化空间。
- 镜头是否清楚、必要且不打断对话。
- 开普勒是否只承担显示导演职责。
- SceneDirector 是否保持隐藏且没有强迫角色服从剧本。

## 15. 核心验收标准

1. 玩家不输入时，当前场景和离屏世界都能产生有因果依据的事件。
2. NPC 可以主动与 NPC 交流，也可以主动询问、呼唤或寻找主角。
3. 暂停后不再推进世界时间、不再发起 LLM 调用，也不产生隐藏世界事件。
4. RP 输入或 STT 期间，全局逻辑时间按输入慢动作倍率推进，当前场景与离屏世界不会继续原速运行。
5. 剧场模式主角能自治；玩家导演指令不会直接显示为角色台词，并能在不改写既有事实的前提下约束后续发展。
6. RP 模式不生成主角主动行为，不展示离屏事实，不泄露章节剧情。
7. SceneDirector 不输出前台正文，开普勒不替角色行动，Character Agent 不负责转场。
8. 连续高压能够触发余波，连续无变化能够触发有因果依据的推进。
9. Branch、WorldRewind 和 SavePoint 按各自语义恢复；固定样本重放不调用 LLM 且得到完全相同的投影。
10. 世界书和记忆检索不会跨角色泄露私有信息。
11. 导入的 SillyTavern 角色卡与世界书能够保留未知扩展字段并按声明范围运行。
12. 自动运行具有调用预算和退避机制，不会因无人输入无限高速消耗模型额度。
13. 节奏状态由确定性事件特征更新，SceneDirector 不能直接改写 `PacingState` 或自评是否需要高潮。
14. `Sustain` 与 `Fade` 不会继续叠加更严重的无因果危机，状态转换不会在相邻事件间反复震荡。
15. Character Agent 收不到章节剧本或越权事实，Director 不能选择不存在、不可达或前置条件未成立的场景与角色。
16. DirectorPulse 覆盖每个前台事件和被提升的重要离屏事件，但不能修改故事线和节奏状态；DirectorReflection 只在记录明确触发原因后运行。
17. `PendingDirection` 不会被静默丢弃；核心意图在最早合法时机实现，无法实现时明确标记阻塞原因。
18. 离屏事实可以延迟切镜，但一旦对当前镜头形成可观察后果，系统必须立即开放并正确叙述相关事实。
19. `game_mode` 在会话创建后不可修改；切换模式只能创建新会话。
20. 调整世界速度或进入输入慢动作后，逻辑世界时间、事件到期和离屏日程按同一倍率变化，而不是只改变界面播放速度。
21. WorldTickGovernor、RevealQueue 和 LLM 预算在长时间无人输入时保持有界，超预算后自动退化为规则模拟。
22. 事件、`StateChangeSet` 和投影以单写者原子提交，不出现重复事件、因果倒序或部分状态生效。
23. `sequence` 只在提交时分配，Draft 不占序号；每个已提交事件显式记录正确的 `world_epoch`。
24. RevealQueue 可完全由事件流和前台事件的 `revealed_event_cursors` 重建，缓存损坏不会导致重复或遗漏揭示。
25. 角色可感知事件能够确定性重算直接观察者；非感知事件允许空集但必须标记 `observability_computed`，不得伪造观察者。

## 16. 主要风险与应对

| 风险 | 后果 | 应对 |
|---|---|---|
| Director 权力过大 | 角色成为剧情木偶 | Director 只分配机会；角色决定具体行为，角色结果优先于计划 |
| 双导演职责重叠 | 重复叙述、互相改写 | 只通过结构化 `BeatPlan` 协作，禁止自由文本协商 |
| 自动推进过快 | 玩家无法阅读或输入 | 全局输入慢动作倍率、动态响应窗口、玩家负荷指标和暂停按钮 |
| 为防拖沓而乱加危机 | 剧情像过山车 | 推进必须来源于既有目标、伏笔、日程或因果 |
| 呼吸节拍变成废话 | 看似变慢但没有内容 | 呼吸场景仍需关系、情绪、信息整理或活动完成 |
| 离屏全量 LLM 模拟 | 成本失控 | 分层精度、事件提升和调用预算 |
| 向量召回泄露信息 | 角色获得全知视角 | 所有向量先按所有者与可见权限过滤，再做相似度检索 |
| LLM 输出结构无效 | 调度器停滞或状态损坏 | Schema 校验、有限重试、确定性降级和幂等提交 |
| Director 自己评价并控制节奏 | 指标漂移且无法复现 | 普通代码负责特征累计和状态转移，Director 只做合法候选的语义选择 |
| 前台呼吸导致世界停止 | 世界仍围绕玩家镜头运转 | 分离世界模拟与呈现循环，分别维护 `world_urgency` 和 `visible_intensity` |
| 完整剧本泄露或生成不存在场景 | 悬念破坏、世界事实断裂 | InformationGate、ActorBrief 和 WorldAffordanceValidator 作为提交硬门 |
| Pulse 越权成为第二个完整导演 | 每事件推翻规划、节奏震荡 | 限定 Pulse Schema 和写权限，故事线修改只允许 Reflection 执行 |
| 玩家导演指令长期不兑现 | 玩家失去导演级控制感 | 持久化 PendingDirection、完成条件、阻塞原因和兑现时限 |
| 延迟离屏镜头掩盖当前后果 | 玩家看到与世界事实矛盾的场景 | RevealQueue 追踪因果距离，可观察后果强制立即揭示 |
| RevealQueue 缓存重建丢失完成状态 | 重复展示或遗漏隐藏事实 | 前台事件持久化 `revealed_event_cursors`，缓存只作可重算派生物 |
| 非感知事件被强行填观察者 | Director 计划或控制事件污染角色记忆 | 允许观察者空集并持久化 `observability_computed`，仅角色可感知事件要求直接观察者 |
| Draft 占用事件序号 | 丢弃草稿造成序列空洞或删除追加事件 | `sequence` 仅由单写者在提交时分配，Draft 永不占号 |
| 世界循环与队列无界增长 | 成本失控、恢复后信息倾泻 | WorldTickGovernor、分层预算、有界 RevealQueue、摘要合并和规则降级 |
| LLM 状态命令破坏重放 | 同一事件恢复出不同世界 | 持久化规范化 StateChangeSet，重放期间禁止调用 LLM |
| 取消域互相污染 | 插话导致世界停止或暂停未生效 | world-loop、plan、generation 三级取消域和明确级联规则 |
| 原样问候没有场景状态 | 后续事件立即违反世界校验 | authored_prologue 反向生成并校验 InitialSceneProposal，缺省字段使用确定性默认值 |
| 前台生成慢于逻辑时钟 | 玩家永久看到滞后世界 | `generation_backpressure_factor` 减慢时钟、`session_time_scale` 上限和滞后诊断指标 |
| 跨 epoch 记忆检索 | 非回归者获得循环外知识 | `(owner_character_id, world_epoch)` 可见集谓词，并作为发布硬门 |
| 章节变体资产随旧存档删除 | 作者内容不可恢复 | P0 导出条件谓词资产，删除动作排在导入之后 |
| 清理旧存档时误删配置 | API、Agent 或导入资源需要重新配置 | 区分游戏存档与用户配置，只清理明确列入旧存档清单的数据 |
| 章节规划压过沙盒行为 | 玩家选择失去意义 | 章节作为软状态机，已提交事件和角色选择拥有更高优先级 |

## 17. 已定案事项

1. 剧场模式玩家输入为导演级创作指令，交给 SceneDirector 和开普勒协调实现，不直接进入角色对话。
2. SillyTavern `first_mes` 和备用问候原样展示，并作为 `ImportedGreeting` 标记来源。
3. 未发布旧版本的游戏存档直接删除；无需自动迁移、只读归档或兼容层。
4. SceneDirector 采用事件级 `DirectorPulse` 与关键节点 `DirectorReflection` 两级运行。
5. `PlayerDirection` 采用因果化强制实现，不能因普通角色自治目标而被忽略。
6. 剧场模式离屏事实由 Director 择机揭示，但当前镜头已经可观察的后果必须立即呈现。
7. 游戏模式在新建会话时选择并永久锁定，同一会话不能切换模式。
8. 世界速度强绑定逻辑世界时间；输入与 STT 期间使用大于零的慢动作倍率同步减速整个世界。
9. 死亡回归只让世界规则指定的回归者保留循环记忆，其他角色记忆随世界回滚。
10. DirectorPulse 覆盖前台事件和重要离屏事件，不覆盖普通离屏日程与规则快进。
11. 世界时钟包含系统计算的 `generation_backpressure_factor`；它与用户设定的世界速度分离，并对玩家可见。
12. 可观察性拆分为 `direct_observers` 与 `potential_learners`，只有前者及镜头内物理痕迹触发强制揭示。
13. 记忆与向量检索按 `(owner_character_id, world_epoch)` 可见集过滤，回归者可访问自己的历史 epoch。
14. 导入角色卡缺少场景字段时使用确定性默认值启动会话，只有显式事实冲突才拒绝。
15. `TimelineEvent` 显式记录 `world_epoch`；`sequence` 仅在提交时分配，Draft 不占用分支序号。
16. 前台事件通过 `revealed_event_cursors` 记录已揭示源事件，RevealQueue 始终可以从事件流确定性重建。
17. 非感知事件允许 `direct_observers` 为空，但必须标记 `observability_computed`；角色可感知事件不得使用空集或占位值逃避计算。

## 18. 研究依据

- Mike Booth, [The AI Systems of Left 4 Dead](https://cdn.akamai.steamstatic.com/apps/valve/2009/ai_systems_of_l4d_mike_booth.pdf)：采用可测 intensity、Build Up/Sustain Peak/Peak Fade/Relax 闭环、结构化不可预测性，以及“调节频率而非难度”的控制边界。
- Hongqiu Wu et al., [Towards Enhanced Immersion and Agency for LLM-based Interactive Drama](https://aclanthology.org/2025.acl-long.546/)（ACL 2025）：采用 Director→Actor 抽象 motivation、Actor 不读取剧本和有限 plot-based reflection；其剧本泄露与不存在场景案例直接支持信息门和世界可行性校验。
- 论文的固定主角、顺序 plot chain、固定步数反思、展平记忆和三场景 Hybrid 结果不直接外推到本项目；本计划只采用经过连续世界、去主角中心和长期记忆约束改造后的机制。
